/**
 * Base URL of the Spring Boot `UserFront` backend. All Admin Portal API
 * calls are session-cookie based and must be made with credentials included
 * (the Angular code used `withCredentials: true`).
 */
export const API_BASE_URL = "http://localhost:8080";
