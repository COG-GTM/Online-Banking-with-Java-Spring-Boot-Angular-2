# Online Banking with Java, Spring Boot, Angular 2

Developed an Banking website which lets you deposit or withdraw money, schedule an appointment with a banker, view bank statements, transfer money between primary or savings account or with other customer.

Created two separate server:

- User Frontend
- Admin Portal

## User Frontend 

User-Front is a user-facing system and it includes modules such as User Signup/Login, Account, Transfer, Appointment, Transaction and User Profile.

## Admin Portal

It is mainly used by Admin and it involves User Account and Appointment modules. Admin can enable/disable Users, view statements of every Users, confirm an appointment.

## Technologies Used

**Front-end:** Html5/CSS3, JavaScript, TypeScript, JQuery, Bootstrap, Angular 2 and some JS plugins, JSON, Thymeleaf

**Back-end:** Java 8, Spring Boot, Spring Data, Spring Security, Hibernate, MySQL, Maven, Log4j

## Software Bill of Materials (SBOM)

CycloneDX SBOMs are provided for each application and as a root aggregate:

- `UserFront/sbom.json` — Maven dependencies for the User Frontend.
- `AdminPortal/sbom.json` — npm dependencies for the Admin Portal.
- `sbom.json` — a merge of the components and dependencies from both component SBOMs.

To regenerate the component SBOMs:

```sh
cd UserFront
mvn -B org.cyclonedx:cyclonedx-maven-plugin:2.7.11:makeAggregateBom -DoutputFormat=json -DschemaVersion=1.5
cp target/bom.json sbom.json

cd ../AdminPortal
npm install --ignore-scripts --legacy-peer-deps
npx --yes @cyclonedx/cyclonedx-npm --spec-version 1.4 --output-file sbom.json --output-reproducible
```

The root `sbom.json` is regenerated by merging the two component SBOMs into a CycloneDX 1.4 document, assigning a new UUID serial number, deduplicating components by `bom-ref` or `purl`, and concatenating their dependency arrays.
